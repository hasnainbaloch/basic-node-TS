// invoice status
export enum InvoiceStatus {
  APPROVED = "approved",
  PENDING = "pending",
}

// invoice payment status
export enum InvoicePaymentStatus {
  PAID = "paid",
  UNPAID = "unpaid",
  OVERDUE = "overdue",
}
